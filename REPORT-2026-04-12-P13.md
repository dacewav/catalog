# DACEWAV.STORE — Session Report P13

**Date:** 2026-04-12 02:15–03:02 GMT+8

---

## ✅ Completed

### 1. DRY Refactor — `_buildCardHTML()`
- Extracted shared card HTML builder function
- `_renderFullPvCard` and `renderFullPvInCard` now thin wrappers (~15 lines each vs ~240 duplicated)
- Both floating preview and embedded preview use same rendering logic
- Eliminates drift between the two previews

### 2. Scoped CSS for Store Card Preview in Admin
- Added `#pv-full-card-container` scoped styles for `.beat-card`, `.beat-card-inner`, `.beat-img`, `.beat-body`, etc.
- Maps admin CSS vars to store CSS vars: `--sf→--surface`, `--b→--border`, `--mu→--muted`, `--tx→--text`, `--fd→--font-d`, `--fm→--font-m`
- Includes glow animations, shimmer, and all animation keyframes

### 3. Resize Direction Fix
- `resize.js`: Changed `diff = startX - e.clientX` → `diff = e.clientX - startX`
- Now drag-right = bigger preview (intuitive)

### 4. Detach Preview with Resize Handles
- Detached card gets resize handles (right edge, bottom edge, corner)
- Handles appear on hover, use PointerEvents API
- Clean removal on re-attach

### 5. Holograma Multi-Color Gradient/Pulse
- Two new modes: `gradient` (animated gradient overlay) and `pulse` (color cycling)
- `holoColors` rendered as CSS gradient via `::after` pseudo-element
- Gradient mode: `linear-gradient(135deg)` with `background-position` animation
- Pulse mode: cycles through colors with `mix-blend-mode: overlay`
- Works in both store (`beat-card`) and admin (`bcpv` mini preview)
- Full keyframes in both `store-styles.css` and `admin.html`

### 6. Critical Bug Fix — `pvDetached` Undeclared
- `pvDetached` was used without declaration after DRY refactor
- In strict ES modules, this threw `ReferenceError` on module load
- Result: entire beats module failed to initialize → no preview, no detach button
- Fixed by adding `var pvDetached = false;`

---

## 🔄 Known Issues for Next Session

### 1. Right Preview Panel Not Visible
- The `.preview-panel` (right side with iframe) is not rendering for the user
- Attempted fixes: changed breakpoint 900→768→removed auto-hide, added `min-width: 300px`, added `min-width: 0` on controls, `overflow: hidden` on `.split`
- Added debug: red border + red background — still nothing visible
- Console shows `[PvRender] done, html length: 3485` — function runs, containers found
- **Root cause unknown** — might be viewport-specific, CSS cascade issue, or Cloudflare edge caching
- The user's viewport width is unknown (couldn't get `window.innerWidth`)
- **Reverted all preview panel changes** — panel is back to original state (iframe only, no store card)

### 2. Store Card Preview in Extras Tab
- The `#pv-full-card-container` in Extras tab should work (scoped CSS + correct var mapping)
- But without being able to see the right panel, untested visually

### 3. Holograma Multi-Color (Unverified)
- Code is in place but untested in production
- Need to verify gradient overlay actually renders correctly

### 4. GitHub Token
- User shared GitHub token multiple times in chat
- Should be rotated

---

## Commits (chronological)
```
014e242 fix: preview card rendering + refactor DRY + resize direction
8ad0929 feat: detach resize handles + holograma multi-color gradient/pulse
2dcdfd7 fix: undeclared pvDetached broke entire admin module
620f302 fix: store card preview colors match + right panel card
7342a32 fix: preview panel layout — show store card above iframe
a9a750f fix: store card as direct child of preview panel, not inside iframe wrap
6034e78 debug: add console.log to renderFullPvInCard + loading placeholder
fbd713c fix: lower preview panel breakpoint from 900px to 768px
7f6fc69 fix: never auto-hide preview panel, just make it narrower on small screens
9fbb322 fix: preview panel guaranteed visible — min-width 300px + controls min-width 0
064f934 debug: red border + red bg to find preview panel
<next>: revert debug, restore original preview panel state
```

---

## Architecture Notes
- `admin.html` — single 2900+ line file with inline CSS, HTML, and scripts
- `src/admin/beats.js` — ~1370 lines, contains card style schema, preview rendering, presets, DRY builder
- `src/admin/resize.js` — preview panel resize handle
- `src/admin/core.js` — `setViewport()`, `toggleInspector()`, main init
- Build: `node build.js` (esbuild, outputs to `dist/`)
- CSS: `store-styles.css` (store) + inline in `admin.html` (admin)
- Deploy: Cloudflare Pages, `dist/` folder

## Next Session TODO
1. **Debug right panel visibility** — try opening admin on a known-wide screen, check `window.innerWidth`, inspect `.split` and `.preview-panel` in DevTools Elements tab
2. Once panel visible, re-add store card preview with correct CSS var mapping
3. Test holograma gradient/pulse modes in production
4. Clean up bcpv CSS duplication (bcpv has its own glow/animation keyframes that duplicate store ones)
