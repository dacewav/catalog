# Report — 2026-04-11

## Estado: FIXES NO FUNCIONARON — Requiere re-trabajo completo

## Problemas activos (SIN SOLUCIONAR)

### 1. Stroke hardcodeado en hero — NO FUNCIONA
- El CSS `.glow-word` sigue aplicando stroke/blur aunque esté desactivado
- `admin.html` línea ~440 y `store-styles.css` tienen estilos que sobreescriben los inline
- El pseudo-element `::after` de `.glow-word` crea blur SIEMPRE
- **Necesita:** Remover TODOS los estilos de `.glow-word` del CSS y dejarlos SOLO en JS inline cuando el usuario activa los efectos
- **O mejor:** No usar `.glow-word` en absoluto si no hay efectos activos — renderizar texto plano

### 2. Admin NO manda cambios a la tienda — NO FUNCIONA
- `postMessage` no está llegando al iframe
- El iframe carga `index.html` pero los mensajes no se procesan
- **Necesita:** Debuggear con `console.log` en ambos lados (admin core.js y store main.js)
- Verificar que el iframe realmente carga (no CSP block)
- Verificar que `window.parent.postMessage` en el store se ejecuta
- Posible problema: Cloudflare caching, CORS, o el iframe src no es correcto

### 3. Store carga muy lento
- `store-app.js` pesa 49KB + Firebase SDK desde CDN
- Posible problema: Firebase init bloquea el render
- **Necesita:** Verificar si Firebase responde rápido o si hay timeout

### 4. Opciones de diseño del texto incompletas
- Falta: color de texto por línea, sombras, más controles de tipografía

## Lo que SÍ se hizo (funciona)
- ✅ Clases CSS faltantes para elementos JS (beat-row, lic-ed, inline-modal, etc.)
- ✅ `X-Frame-Options: DENY` removido de `_headers` (iframe puede cargar)
- ✅ Init order fix — `buildColorEditor` corre antes de `loadThemeUI`
- ✅ R2 config se auto-guarda con otros settings
- ✅ Repo limpiado (prompts, reports viejos, archivos accidentales)
- ✅ `body.light` mode CSS

## Archivos clave para el siguiente chat

| Archivo | Qué hacer |
|---------|-----------|
| `admin.html` CSS | Remover TODOS los `.glow-word` styles hardcodeados |
| `store-styles.css` | Remover `.glow-word::after` o default a opacity 0 |
| `src/admin/core.js` | Revisar `broadcastTheme()` — debug postMessage |
| `src/admin/core.js` | Reescribir `updateHeroPv()` sin glow-word si no hay efectos |
| `src/settings.js` | Revisar `applySettings()` hero rendering |
| `src/theme.js` | Revisar `applyTheme()` hero styling |
| `src/main.js` | Revisar message listener — puede que no reciba mensajes |
| `_headers` | Verificar CSP no bloquea iframe communication |

## Commits de esta sesión
```
70a9f80 fix: remove hardcoded stroke, fix store communication, add report
060c9b8 fix: hero text fully customizable, no hardcoded stroke
6aa7bf8 fix: init order — buildColorEditor before loadThemeUI
d69bf55 fix: preview iframe, hero rewrite, auto-save R2 config
5198a4e fix: add missing CSS classes for JS-generated elements
```

## Token GitHub
- El usuario rotará el token después de esta sesión
- Usar `gh` CLI o token nuevo en el siguiente chat
