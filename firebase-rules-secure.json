{
  "rules": {
    ".read": false,
    ".write": false,

    "adminWhitelist": {
      ".read": "auth != null",
      ".write": "auth.uid === 'Uks9YGSd6rS40zqlRujoe6pE6N22' || (auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists())",
      "approved": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth.uid === 'Uks9YGSd6rS40zqlRujoe6pE6N22' || (auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists())"
        }
      },
      "pending": {
        ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
        ".write": "auth.uid === 'Uks9YGSd6rS40zqlRujoe6pE6N22' || (auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists())"
      }
    },

    "settings": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "theme": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "beats": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      "$beatId": {
        ".validate": "newData.hasChildren(['name', 'genre', 'bpm', 'key'])",
        "name": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 200" },
        "genre": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 100" },
        "genreCustom": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 100)" },
        "bpm": { ".validate": "newData.isNumber() && newData.val() >= 20 && newData.val() <= 400" },
        "key": { ".validate": "newData.isString() && newData.val().length <= 10" },
        "description": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 2000)" },
        "tags": { ".validate": "!newData.exists() || newData.isString()" },
        "imageUrl": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 2000)" },
        "images": { ".validate": "!newData.exists() || newData.isString()" },
        "audioUrl": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 2000)" },
        "previewUrl": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 2000)" },
        "spotify": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 500)" },
        "youtube": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 500)" },
        "soundcloud": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 500)" },
        "date": { ".validate": "!newData.exists() || newData.isString()" },
        "order": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0)" },
        "active": { ".validate": "newData.isBoolean()" },
        "featured": { ".validate": "newData.isBoolean()" },
        "exclusive": { ".validate": "newData.isBoolean()" },
        "available": { ".validate": "newData.isBoolean()" },
        "_customStyle": { ".validate": "!newData.exists() || newData.isBoolean()" },

        "plays": { ".read": true, ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()", ".validate": "newData.isNumber() && newData.val() >= 0" },
        "clicks": { ".read": true, ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()", ".validate": "newData.isNumber() && newData.val() >= 0" },
        "views": { ".read": true, ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()", ".validate": "newData.isNumber() && newData.val() >= 0" },
        "waClicks": { ".read": true, ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()", ".validate": "newData.isNumber() && newData.val() >= 0" },

        "licenses": {
          "$licIdx": {
            "name": { ".validate": "newData.isString() && newData.val().length <= 100" },
            "priceMXN": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "priceUSD": { ".validate": "newData.isNumber() && newData.val() >= 0" },
            "description": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 500)" }
          }
        },

        "glowConfig": {
          "enabled": { ".validate": "newData.isBoolean()" },
          "type": { ".validate": "newData.isString() && newData.val().length <= 30" },
          "color": { ".validate": "newData.isString() && newData.val().length <= 20" },
          "speed": { ".validate": "newData.isNumber() && newData.val() >= 0.1 && newData.val() <= 30" },
          "intensity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 10)" },
          "blur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 200)" },
          "spread": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 100)" },
          "opacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
          "hoverOnly": { ".validate": "!newData.exists() || newData.isBoolean()" }
        },

        "cardAnim": {
          ".validate": "!newData.exists() || (newData.hasChildren(['type', 'dur', 'del']))",
          "type": { ".validate": "newData.isString() && newData.val().length <= 30" },
          "dur": { ".validate": "newData.isNumber() && newData.val() >= 0.1 && newData.val() <= 30" },
          "del": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 10" }
        },

        "accentColor": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
        "shimmer": { ".validate": "!newData.exists() || newData.isBoolean()" },
        "cardBorder": {
          "enabled": { ".validate": "newData.isBoolean()" },
          "color": { ".validate": "newData.isString() && newData.val().length <= 20" },
          "width": { ".validate": "newData.isNumber() && newData.val() >= 0.5 && newData.val() <= 10" },
          "style": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 15)" }
        },

        "cardStyle": {
          "filter": {
            "brightness": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 5" },
            "contrast": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 5" },
            "saturate": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 10" },
            "grayscale": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "sepia": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "hueRotate": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 360)" },
            "blur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 50)" },
            "blurType": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "invert": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "opacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "dropShadowX": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -80 && newData.val() <= 80)" },
            "dropShadowY": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -80 && newData.val() <= 80)" },
            "dropShadowBlur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 100)" },
            "dropShadowOpacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "dropShadowColor": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" }
          },
          "glow": {
            "enabled": { ".validate": "newData.isBoolean()" },
            "type": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 30)" },
            "color": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "speed": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.1 && newData.val() <= 30)" },
            "intensity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 10)" },
            "blur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 200)" },
            "spread": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 100)" },
            "opacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "hoverOnly": { ".validate": "!newData.exists() || newData.isBoolean()" }
          },
          "anim": {
            "type": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 30)" },
            "type2": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 30)" },
            "dur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.1 && newData.val() <= 15)" },
            "del": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 10)" },
            "easing": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 60)" },
            "direction": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 25)" },
            "iterations": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 10)" },
            "intensity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 200)" },
            "hueStart": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 360)" },
            "hueEnd": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 360)" },
            "holoColors": { ".validate": "!newData.exists() || newData.isString()" },
            "holoBrightMin": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "holoBrightMax": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "holoSatMin": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "holoSatMax": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "holoGlow": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 50)" },
            "holoBlur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 20)" },
            "holoDir": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "brilloMin": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "brilloMax": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "glitchX": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 30)" },
            "glitchY": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 30)" },
            "glitchRot": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 15)" },
            "glitchChromatic": { ".validate": "!newData.exists() || newData.isBoolean()" },
            "translateX": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -50 && newData.val() <= 50)" },
            "translateY": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -50 && newData.val() <= 50)" },
            "translateRot": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -15 && newData.val() <= 15)" },
            "neonMin": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "neonMax": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 2)" },
            "neonBright": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "parpadeoMin": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "parpadeoMax": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "rotateAngle": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 45)" },
            "rotateScale": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.5 && newData.val() <= 1.5)" },
            "scaleMin": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.5 && newData.val() <= 1)" },
            "scaleMax": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 1 && newData.val() <= 1.5)" },
            "scaleOpacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "shakeX": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 30)" },
            "shakeY": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 30)" },
            "csHueStart": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 360)" },
            "csHueEnd": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 360)" },
            "csSat": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" }
          },
          "style": {
            "accentColor": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "shimmer": { ".validate": "!newData.exists() || newData.isBoolean()" },
            "shimmerSpeed": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.1 && newData.val() <= 20)" },
            "shimmerOp": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 0.5)" },
            "borderRadius": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 80)" },
            "opacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.05 && newData.val() <= 1)" }
          },
          "border": {
            "enabled": { ".validate": "newData.isBoolean()" },
            "color": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "width": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.5 && newData.val() <= 10)" },
            "style": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 15)" }
          },
          "shadow": {
            "enabled": { ".validate": "newData.isBoolean()" },
            "color": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "opacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "x": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -50 && newData.val() <= 50)" },
            "y": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -50 && newData.val() <= 50)" },
            "blur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 100)" },
            "spread": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -30 && newData.val() <= 60)" },
            "inset": { ".validate": "!newData.exists() || newData.isBoolean()" }
          },
          "hover": {
            "scale": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.5 && newData.val() <= 1.5)" },
            "brightness": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 3)" },
            "saturate": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 5)" },
            "shadowBlur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 100)" },
            "transition": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 2)" },
            "borderColor": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 20)" },
            "glowIntensify": { ".validate": "!newData.exists() || newData.isBoolean()" },
            "blur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 30)" },
            "siblingsBlur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 20)" },
            "hueRotate": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 360)" },
            "opacity": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0 && newData.val() <= 1)" },
            "enableAnim": { ".validate": "!newData.exists() || newData.isBoolean()" },
            "animType": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 30)" },
            "animDur": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.1 && newData.val() <= 10)" }
          },
          "transform": {
            "rotate": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -45 && newData.val() <= 45)" },
            "scale": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= 0.5 && newData.val() <= 1.5)" },
            "skewX": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -30 && newData.val() <= 30)" },
            "skewY": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -30 && newData.val() <= 30)" },
            "x": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -50 && newData.val() <= 50)" },
            "y": { ".validate": "!newData.exists() || (newData.isNumber() && newData.val() >= -50 && newData.val() <= 50)" }
          }
        },

        "$other": { ".validate": "newData.isString() || newData.isNumber() || newData.isBoolean()" }
      }
    },

    "floatingElements": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "liveEdits": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "imageGallery": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      "$imgId": {
        "url": { ".validate": "newData.isString() && newData.val().length <= 2000" },
        "name": { ".validate": "newData.isString() && newData.val().length <= 200" },
        "type": { ".validate": "newData.isString() && newData.val().length <= 30" },
        "size": { ".validate": "!newData.exists() || newData.isNumber()" },
        "width": { ".validate": "!newData.exists() || newData.isNumber()" },
        "height": { ".validate": "!newData.exists() || newData.isNumber()" },
        "mimeType": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 50)" },
        "createdAt": { ".validate": "!newData.exists() || newData.isNumber()" },
        "tags": { ".validate": "!newData.exists() || newData.isString()" },
        "$other": { ".validate": "newData.isString() || newData.isNumber()" }
      }
    },

    "customLinks": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "customEmojis": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "team": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "defaultLicenses": {
      ".read": true,
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "analytics": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      "events": {
        "$date": {
          "$eventId": {
            ".write": true,
            ".validate": "newData.hasChildren(['ts', 'cat', 'act']) && newData.child('ts').val() <= (now + 60000)",
            "ts": { ".validate": "newData.isNumber()" },
            "cat": { ".validate": "newData.isString() && newData.val().length <= 50" },
            "act": { ".validate": "newData.isString() && newData.val().length <= 100" },
            "lbl": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 200)" },
            "val": { ".validate": "!newData.exists() || newData.isNumber()" },
            "meta": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 500)" }
          }
        }
      },
      "counts": {
        "$beatId": {
          ".write": true,
          "clicks": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000000" },
          "views": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000000" },
          "waClicks": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 1000000" }
        }
      },
      "daily": {
        "$date": {
          ".write": true,
          "total": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000" },
          "actions": {
            "$action": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100000" }
          }
        }
      }
    },

    "subscribers": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      "$email": {
        ".validate": "newData.hasChildren(['email', 'addedAt'])",
        "email": { ".validate": "newData.isString() && newData.val().length <= 200" },
        "addedAt": { ".validate": "newData.isNumber()" },
        "name": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 200)" },
        "source": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 50)" },
        "active": { ".validate": "!newData.exists() || newData.isBoolean()" },
        "$other": { ".validate": "newData.isString() || newData.isNumber() || newData.isBoolean()" }
      }
    },

    "contactMessages": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      ".write": true,
      "$msgId": {
        ".validate": "newData.hasChildren(['name', 'email', 'message', 'ts'])",
        "name": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 200" },
        "email": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 200" },
        "message": { ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 5000" },
        "ts": { ".validate": "newData.isNumber() && newData.val() <= (now + 60000)" },
        "beatId": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 100)" },
        "read": { ".validate": "!newData.exists() || newData.isBoolean()" },
        "$other": { ".validate": false }
      }
    },

    "notifications": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    },

    "backups": {
      ".read": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()",
      ".write": "auth != null && root.child('adminWhitelist/approved').child(auth.uid).exists()"
    }
  }
}
