# 2026-04-17 — Session Log

## Session Start: 22:58 GMT+8

## Objetivo
Optimizar admin + index para que coexistan perfectamente. Admin estaba roto desde hace mucho: botones rotos, tarjetas no aplican diseño, cosas hardcodeadas.

## Diagnóstico (completo)
Ver análisis en MEMORY.md sección "Problemas encontrados"

### Problemas raíz:
1. Dos sistemas cardStyle paralelos (f- prefix vs g- prefix)
2. Campos legacy (glowConfig, cardAnim, etc.) nunca limpiados
3. Preview admin ≠ render store (DOM distinto)
4. openEditor duplica 80+ fields manualmente
5. liveEdits overlay genera confusión

## Plan de refactor (ver REFACTOR-PLAN.md)

### Fase 1: Unificar lectura de cardStyle
- beats.js: usar populateFromCardStyle(cs, 'f-') en vez de 80+ setVal
- Eliminar _buildCardStyleFromInputs duplicado → usar buildCardStyleFromPrefix('f-')

### Fase 2: Unificar aplicación CSS
- Crear applyCardStyle(el, cs) que funcione en preview Y store
- Mismo pipeline CSS en ambos lados

### Fase 3: Limpiar legacy fields
- Store: eliminar fallback a glowConfig/cardAnim/accentColor/shimmer/cardBorder
- Beats.js save: no guardar legacy fields

### Fase 4: Fix preview ↔ store sync
- Limpiar iframe listener inútil en index.html
- Simplificar liveEdits

### Fase 5: Build + Test + Push

## Git token configured (session only)
- Token será revocado al terminar sesión
