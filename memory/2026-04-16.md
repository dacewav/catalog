# 2026-04-16 — Session log

## [11:36] Sesión iniciada
- Clonado repo dacewav/catalog en workspace
- npm install, build, tests verificados — 118/118 OK
- Token GitHub configurado para push

## [11:38] Trabajando en pendientes

### Pendiente 1: Glow/Shimmer no visible en store
- Análisis profundo: flujo de datos admin → store parece correcto en código
- **Fix shimmer**: `will-change:transform` en `.beat-card-inner` puede causar que box-shadow animado desaparezca en GPU layers → removido para cards con animaciones
- **Shimmer hover**: cambiado de `beat-card:hover` (todas las cards) a `beat-card.shimmer-on:hover` (solo shimmer-on)
- **Shimmer opacidad**: gradient 0.06 → 0.08, sweep más amplio (30-70% vs 35-65%)
- **Glow**: validación de tipo glow contra CSS classes conocidas
- Commits: `f6435ed`, `a9aa217`

### Pendiente 2: Shimmer/Shadow/Hover CSS specificity
- Shimmer hover specificity fix (solo shimmer-on cards)
- Custom shadow cards: `has-custom-shadow:not(.has-hover-fx):hover` → brightness(1.08)
- Commit: `f6435ed`

### Pendiente 3: Duplicate class attributes
- 26 instancias de `class="card" class="mt-sm"` en admin.html → mergeadas
- 5 instancias adicionales de otros duplicados → mergeadas
- Los espaciados (mt-sm, mt-md, mb-md) estaban siendo ignorados por el browser
- Commit: `f6435ed`

### Pendiente 4: Firebase Analytics PERMISSION_DENIED
- **Root cause**: `state.db.ServerValue?.TIMESTAMP` → undefined (ServerValue está en firebase.database namespace, no en db instance)
- Events caían a `Date.now()` → timestamp validation fallaba si clock skew
- **Fix**: `firebase.database?.ServerValue?.TIMESTAMP` con fallback a Date.now()
- Commit: pendiente

### Pendiente 5: CSS filters en Media tab
- Verificado: todos los filtros CSS están correctamente dentro de `#etp-extras`
- Los duplicate class attrs fix podría haber resuelto espaciados que causaban confusión visual

## Commits de esta sesión
1. `f6435ed` — fix: shimmer hover specificity, custom shadow hover, glow validation, duplicate class attrs
2. `a9aa217` — fix(store): remove will-change from glow/animated cards, bump shimmer visibility

## Pendientes para próxima sesión
- [ ] Confirmar que shimmer/glow se ven en la store post-deploy
- [ ] Rediseño de la store (feature principal)
- [ ] Glow no visible en iframe preview (puede necesitar más debugging en browser)
- [ ] Revisar si effects como glow, anim, shadow funcionan correctamente en la store

## Estado final
- Build: store 65.9KB, admin 251.8KB
- Tests: 118/118
- Deploy: Cloudflare Pages auto-deploy desde main
